## Snippets

    // lib/strategies/session.js
    // pause stream to prevent event from triggering while authenticating user.
    var paused = options.pauseStream ? pause(req) : null;



todos:
  done:

  pendings:
    what is http.IncomingMessage
    clear todos
    how make node is used.

    app.use(connect.session({ secret: 'keyboard cat' }));
      secret what for?
